export class Car {
    licensePlate: string;
    model: string;
    year: string;

    constructor(licensePlate: string, model: string, year: string) {
        this.licensePlate = licensePlate;
        this.model = model;
        this.year = year;
    }
}

